# فروشگاه جنگو با تقویم جلالی

یک پروژه Django با پشتیبانی کامل از تقویم جلالی (شمسی) در پنل مدیریت

## ویژگی‌های اصلی

### ✨ پشتیبانی کامل از تقویم جلالی
- نمایش تمام تاریخ‌ها به صورت شمسی در پنل ادمین
- ورودی مستقیم تاریخ به صورت جلالی (بدون نیاز به تبدیل)
- تقویم شمسی برای انتخاب تاریخ
- دکمه "اکنون" که به درستی تاریخ و زمان جلالی را تنظیم می‌کند

### 📅 امکانات ویژه
- **ویرایش تاریخ**: امکان ویرایش دستی فیلدهای تاریخ
- **تاریخچه تغییرات**: نمایش تاریخچه ویرایش‌ها با تاریخ شمسی
- **قیمت به تومان**: نمایش قیمت با جداکننده هزارگان و واحد تومان
- **رابط کاربری فارسی**: تمام بخش‌های مدیریت به زبان فارسی

## نصب و راه‌اندازی

### پیش‌نیازها

- Python 3.8 یا بالاتر
- pip (مدیر بسته‌های Python)
- virtualenv (اختیاری اما توصیه می‌شود)

### مراحل نصب

1. **کلون کردن پروژه:**
```bash
git clone https://github.com/BenyNotNice/django-store.git
cd django-store
```

2. **ساخت محیط مجازی:**
```bash
python -m venv venv
source venv/bin/activate  # در macOS/Linux
# یا
venv\Scripts\activate  # در Windows
```

3. **نصب وابستگی‌ها:**
```bash
pip install -r requirements.txt
```

4. **اجرای مایگریشن‌ها:**
```bash
python manage.py migrate
```

5. **ساخت کاربر مدیر:**
```bash
python manage.py createsuperuser
```
مشخصات خود را وارد کنید (نام کاربری، ایمیل، رمز عبور)

6. **اجرای سرور:**
```bash
python manage.py runserver
```

7. **دسترسی به پنل مدیریت:**
- آدرس: http://127.0.0.1:8000/admin/
- با نام کاربری و رمز عبوری که ساختید وارد شوید

## نحوه استفاده

### افزودن محصول جدید

1. وارد پنل ادمین شوید
2. روی **محصولات** کلیک کنید
3. دکمه **افزودن محصول** را بزنید
4. مشخصات محصول را وارد کنید:
   - **نام محصول**: نام محصول به فارسی
   - **قیمت**: قیمت به تومان (عدد، بدون جداکننده)
   - **تاریخ ایجاد**: می‌توانید تاریخ را تایپ کنید یا از تقویم انتخاب کنید
   - **فعال**: برای نمایش محصول فعال کنید

### تاریخ‌گذاری

**روش 1: استفاده از دکمه "اکنون"**
- روی دکمه "اکنون" کلیک کنید
- تاریخ و زمان فعلی به صورت خودکار وارد می‌شود

**روش 2: تایپ مستقیم**
- فرمت تاریخ: `YYYY-MM-DD` (مثال: `1403-09-07`)
- فرمت زمان: `HH:MM:SS` (مثال: `14:30:00`)

**روش 3: انتخاب از تقویم**
- روی آیکون تقویم 📅 کلیک کنید
- تاریخ مورد نظر را انتخاب کنید

### مشاهده تاریخچه تغییرات

1. وارد صفحه ویرایش یک محصول شوید
2. دکمه **تاریخچه** را بزنید
3. تمام تغییرات با تاریخ و زمان شمسی نمایش داده می‌شود

## ساختار پروژه

```
store/
├── config/              # تنظیمات اصلی پروژه
│   ├── settings.py     # تنظیمات Django و جلالی
│   └── urls.py         # مسیریابی اصلی
├── store/              # اپلیکیشن فروشگاه
│   ├── models.py       # مدل‌ها (محصولات)
│   ├── admin.py        # پیکربندی پنل ادمین
│   ├── static/         # فایل‌های استاتیک
│   │   └── admin/js/
│   │       └── jalali_now_fix.js  # اصلاح دکمه "اکنون"
│   ├── templates/      # قالب‌های سفارشی
│   │   └── admin/
│   │       └── object_history.html
│   └── templatetags/   # فیلترهای سفارشی
│       └── jalali_filters.py
├── requirements.txt    # وابستگی‌های پروژه
└── README.md          # این فایل
```

## تکنولوژی‌های استفاده شده

- **Django 5.0.1**: فریمورک اصلی
- **django-jalali**: پشتیبانی از تاریخ جلالی در مدل‌ها
- **django-jalali-date**: ویجت‌های تاریخ جلالی برای پنل ادمین
- **jdatetime**: کتابخانه تبدیل تاریخ
- **SQLite**: پایگاه داده (می‌توان به PostgreSQL تغییر داد)

## مستندات تکمیلی

- [راهنمای پیاده‌سازی تکنیکی](JALALI_IMPLEMENTATION.md)
- [راهنمای کامل کاربر](USAGE_GUIDE.md)

## ماه‌های تقویم شمسی

| شماره | نام ماه    | تعداد روز |
|-------|-----------|----------|
| 01    | فروردین   | 31       |
| 02    | اردیبهشت  | 31       |
| 03    | خرداد     | 31       |
| 04    | تیر       | 31       |
| 05    | مرداد     | 31       |
| 06    | شهریور    | 31       |
| 07    | مهر       | 30       |
| 08    | آبان      | 30       |
| 09    | آذر       | 30       |
| 10    | دی        | 30       |
| 11    | بهمن      | 30       |
| 12    | اسفند     | 29/30*   |

*اسفند در سال‌های کبیسه 30 روز دارد

## مشکلات رایج و راه‌حل

### خطای "Enter a valid date"

**مشکل**: تاریخ وارد شده معتبر نیست

**راه‌حل**:
- از فرمت `YYYY-MM-DD` استفاده کنید
- ماه باید بین 1 تا 12 باشد
- روز باید معتبر باشد (مثلاً مهر فقط 30 روز دارد)

### تاریخ‌ها میلادی نمایش داده می‌شود

**راه‌حل**:
- کش مرورگر را پاک کنید (Ctrl+Shift+R یا Cmd+Shift+R)
- سرور را مجدداً راه‌اندازی کنید
- مطمئن شوید که در تنظیمات `USE_TZ = True` است

### دکمه "اکنون" کار نمی‌کند

**راه‌حل**:
- کش مرورگر را پاک کنید
- کنسول JavaScript را برای خطاها بررسی کنید
- مطمئن شوید فایل `jalali_now_fix.js` بارگذاری شده است

## توسعه و مشارکت

### اجرای تست‌ها

```bash
python manage.py test
```

### افزودن ویژگی جدید

1. یک branch جدید بسازید:
```bash
git checkout -b feature/new-feature
```

2. تغییرات خود را commit کنید:
```bash
git add .
git commit -m "Add new feature"
```

3. به مخزن اصلی push کنید:
```bash
git push origin feature/new-feature
```

4. یک Pull Request ایجاد کنید

## لایسنس

این پروژه تحت لایسنس MIT منتشر شده است - برای جزئیات بیشتر فایل [LICENSE](LICENSE) را مشاهده کنید.

## نویسنده

**بنیامین** - [BenyNotNice](https://github.com/BenyNotNice)

## قدردانی

- تیم Django برای فریمورک عالی
- توسعه‌دهندگان django-jalali و django-jalali-date
- جامعه متن‌باز ایران

## تماس و پشتیبانی

- **Issues**: برای گزارش باگ یا درخواست ویژگی از [GitHub Issues](https://github.com/BenyNotNice/django-store/issues) استفاده کنید
- **Discussions**: برای پرسش و گفتگو از [GitHub Discussions](https://github.com/BenyNotNice/django-store/discussions) استفاده کنید

---

**نکته**: این پروژه در حال توسعه است. برای آخرین تغییرات، branch `main` را دنبال کنید.

## نسخه‌ها

- **v1.0.0** (فعلی)
  - پشتیبانی کامل از تقویم جلالی
  - دکمه "اکنون" کارآمد
  - تاریخچه تغییرات با تاریخ شمسی
  - رابط کاربری کامل فارسی

## نقشه راه

- [ ] افزودن فیلتر تاریخ در لیست محصولات
- [ ] گزارش‌گیری با تاریخ شمسی
- [ ] صفحه فروشگاه عمومی
- [ ] سیستم سفارش‌گذاری
- [ ] پنل کاربری

---

ساخته شده با ❤️ برای جامعه توسعه‌دهندگان ایرانی
