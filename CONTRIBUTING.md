# راهنمای مشارکت در پروژه

از اینکه علاقه‌مند به مشارکت در این پروژه هستید متشکریم! 🎉

## نحوه مشارکت

### گزارش باگ

اگر باگی پیدا کردید:

1. ابتدا [Issues موجود](https://github.com/BenyNotNice/django-store/issues) را بررسی کنید
2. اگر باگ گزارش نشده، یک Issue جدید ایجاد کنید
3. در گزارش خود شامل کنید:
   - توضیح مشکل
   - مراحل بازتولید باگ
   - رفتار مورد انتظار
   - رفتار فعلی
   - اسکرین‌شات (در صورت امکان)
   - محیط شما (نسخه Python، Django، OS)

### پیشنهاد ویژگی جدید

برای پیشنهاد ویژگی:

1. یک Issue با برچسب "enhancement" ایجاد کنید
2. توضیح دهید که چرا این ویژگی مفید است
3. مثال‌های کاربردی ارائه دهید
4. اگر امکان دارد، طرح پیاده‌سازی را توضیح دهید

### ارسال Pull Request

1. **Fork کردن پروژه**
   ```bash
   # در GitHub روی دکمه Fork کلیک کنید
   ```

2. **کلون کردن Fork خود**
   ```bash
   git clone https://github.com/YOUR_USERNAME/django-store.git
   cd django-store
   ```

3. **ساخت Branch جدید**
   ```bash
   git checkout -b feature/your-feature-name
   # یا
   git checkout -b fix/bug-description
   ```

4. **نصب محیط توسعه**
   ```bash
   python -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   python manage.py migrate
   ```

5. **انجام تغییرات**
   - کد خود را بنویسید
   - از استانداردهای کدنویسی پیروی کنید
   - تست‌های لازم را اضافه کنید

6. **تست کردن تغییرات**
   ```bash
   python manage.py test
   python manage.py runserver  # تست دستی
   ```

7. **Commit کردن**
   ```bash
   git add .
   git commit -m "feat: توضیح مختصر تغییرات"
   ```

8. **Push کردن**
   ```bash
   git push origin feature/your-feature-name
   ```

9. **ایجاد Pull Request**
   - به مخزن اصلی بروید
   - دکمه "New Pull Request" را کلیک کنید
   - توضیحات کامل ارائه دهید

## استانداردهای کدنویسی

### Python/Django

- از [PEP 8](https://pep8.org/) پیروی کنید
- از docstring برای توابع و کلاس‌ها استفاده کنید
- نام متغیرها باید واضح و معنادار باشد

**مثال:**
```python
def calculate_total_price(items):
    """
    محاسبه قیمت کل آیتم‌ها

    Args:
        items (list): لیست آیتم‌ها با قیمت

    Returns:
        int: مجموع قیمت‌ها
    """
    return sum(item.price for item in items)
```

### JavaScript

- از ES6+ استفاده کنید
- کامنت‌های واضح بنویسید
- نام متغیرها camelCase باشد

**مثال:**
```javascript
/**
 * تبدیل تاریخ میلادی به جلالی
 * @param {Date} gregorianDate - تاریخ میلادی
 * @returns {Object} - تاریخ جلالی
 */
function convertToJalali(gregorianDate) {
    // کد تبدیل
}
```

### Commit Messages

از [Conventional Commits](https://www.conventionalcommits.org/) استفاده کنید:

```
feat: افزودن ویژگی جدید
fix: رفع باگ در تبدیل تاریخ
docs: بهبود مستندات
style: اصلاح فرمت کد
refactor: بازنویسی تابع تبدیل
test: افزودن تست برای مدل محصول
chore: بروزرسانی وابستگی‌ها
```

## ساختار پروژه

```
django-store/
├── config/              # تنظیمات پروژه
├── store/              # اپلیکیشن اصلی
│   ├── models.py       # مدل‌های دیتابیس
│   ├── admin.py        # پنل ادمین
│   ├── views.py        # ویوها
│   ├── static/         # فایل‌های استاتیک
│   ├── templates/      # قالب‌ها
│   └── templatetags/   # تگ‌های سفارشی
├── tests/              # تست‌ها
├── docs/               # مستندات
└── requirements.txt    # وابستگی‌ها
```

## چک‌لیست Pull Request

قبل از ارسال PR، مطمئن شوید که:

- [ ] کد شما تست شده است
- [ ] تمام تست‌ها پاس می‌شوند
- [ ] مستندات بروزرسانی شده (در صورت نیاز)
- [ ] کد از استانداردها پیروی می‌کند
- [ ] Commit messageها واضح هستند
- [ ] تغییرات شما با branch `main` merge می‌شود

## نیاز به کمک؟

- [GitHub Discussions](https://github.com/BenyNotNice/django-store/discussions) - برای سوالات
- [GitHub Issues](https://github.com/BenyNotNice/django-store/issues) - برای باگ‌ها و پیشنهادات

## قوانین رفتاری

### رفتار مورد انتظار

- احترام به دیگران
- پذیرش نقد سازنده
- تمرکز روی بهترین راه‌حل برای پروژه

### رفتار غیرقابل قبول

- زبان توهین‌آمیز یا تحقیرکننده
- حمله شخصی
- آزار و اذیت

## لایسنس

با مشارکت در این پروژه، موافقت می‌کنید که مشارکت شما تحت [MIT License](LICENSE) باشد.

---

از مشارکت شما متشکریم! 🙏
